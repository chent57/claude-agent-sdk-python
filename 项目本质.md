# Claude Agent SDK for Python - 项目本质

## 一句话概括

这是一个 **Claude Code CLI 的 Python 封装 SDK**，通过子进程与本地 CLI 进行双向通信。

## 工作原理

```
Python 应用
    ↓
ClaudeSDKClient / query()
    ↓
SubprocessCLITransport
    ↓ (启动子进程，通过 stdin/stdout 通信)
Claude Code CLI (本地安装)
    ↓
Claude API
```

## 核心架构

```
Public API (query, ClaudeSDKClient)
         ↓
Internal Coordination (InternalClient)
         ↓
Control Protocol (Query - 双向通信/hooks/权限)
         ↓
Transport Layer (SubprocessCLITransport)
         ↓
Claude Code CLI (外部进程)
```

## 目录结构

```
src/claude_agent_sdk/
├── __init__.py          # 主导出和MCP工具装饰器
├── client.py            # ClaudeSDKClient 双向交互客户端
├── query.py             # 一次性查询函数
├── types.py             # 所有类型定义
├── _errors.py           # 错误类型
└── _internal/           # 内部实现
    ├── client.py        # 内部客户端协调
    ├── query.py         # 控制协议实现
    ├── message_parser.py # 消息解析
    └── transport/
        └── subprocess_cli.py  # CLI子进程管理
```

## 核心组件

| 组件 | 职责 |
|------|------|
| `query()` | 简单的一次性查询接口 |
| `ClaudeSDKClient` | 有状态的双向交互客户端 |
| `SubprocessCLITransport` | 管理 Claude Code CLI 子进程通信 |
| `Query` 类 | 控制协议处理、Hook、权限回调 |
| `MessageParser` | JSON → 类型化 Python 对象 |

## CLI 查找优先级

SDK 按以下顺序查找 Claude Code CLI：

1. **捆绑的 CLI** (`_bundled/` 目录) - SDK 自带
2. **系统 CLI** - `claude` 命令在 PATH 中
3. **预设位置** - 常见安装路径

## 通信方式

- 使用 `--print-control-protocol json` 启动 CLI
- 通过 **stdin** 发送 JSON 消息
- 通过 **stdout** 接收 JSON 响应
- 支持双向控制协议（hooks、权限回调等）

## 关键特性

- **异步设计**: 全部使用 `async/await`，支持 asyncio/trio
- **类型安全**: mypy strict 模式
- **MCP 支持**: 外部服务器 + SDK 内嵌工具
- **Hook 系统**: 6 种事件（PreToolUse、PostToolUse、UserPromptSubmit 等）
- **权限回调**: `can_use_tool` 细粒度控制

## 依赖

- `anyio` - 异步 I/O
- `mcp` - Model Context Protocol
- `typing_extensions` - 类型注解扩展

## 使用示例

```python
from claude_agent_sdk import query

async for message in query("What is 2+2?"):
    print(message)  # CLI 执行后返回的响应
```
